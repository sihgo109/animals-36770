<h1>トップページ</h1>
<div class="contents row">
  <% @animals.each do |animal| %>
    <div class="content_post" style="background-image: url(<%= animal.image %>);">
      <p><%= animal.catch_copy %></p>
      <span class="name">
        <%= animal.anm_name %>
      </span>
    </div>
  <% end %>
</div>